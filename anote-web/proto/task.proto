

proto Task {
  optional string title = 1;
  optional string note = 2;
  optional int64 complete_at = 3;
  optional int64 created_at = 4;
  optional int64 due_date = 5;
  optional int32 id = 6;
  optional int32 project_id = 7;
  optional int32 parent_id = 8;
  optional int32 position = 9; // 10.00009, we can use 
  optional recurrence_count = 10;
  optional recurrence_type = 11;
  optional recurrence_parent_id = 12;
  optional deleted_at = 13;
  optional string type = 14; // "task"
  optional updated_at = 15;
  optional 
}